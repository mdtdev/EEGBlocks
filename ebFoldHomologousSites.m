function eegDataBlob = ebFoldHomologousSites(eegDataBlob, appfunc, order)

% function eegDataBlob = ebFoldHomologousSites(eegDataBlob, appfunc, order)
%
% Applies function handle passed as "appfunc" (also called OP) to the
% homologous sites. Remember to pass it as a handle! Assumes "@minus" if no
% value is passed. Also note that custom functions can be passed if they
% are defined, as well. Default order (minus) is left from right (right OP
% left or right power - left power).
%
% STUB!
%
% MDT
% 2016.03.28
% Version 0.0.1



end